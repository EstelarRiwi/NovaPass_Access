services:
  app:
    image: ghcr.io/estelarriwi/novapass_access:latest
    pull_policy: always
    env_file:
      - .env
    volumes:
      - app-storage:/var/www/storage
    ports:
      - "9001:9000"
    restart: unless-stopped

volumes:
  app-storage:
